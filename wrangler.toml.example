name = "fitbit-googlehealth-mcp"
main = "src/index.ts"
compatibility_date = "2025-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Anthropic's published outbound CIDR for Claude → MCP server connections.
ALLOWED_CIDRS = "160.79.104.0/21"

# Create both KV namespaces before `wrangler deploy`:
#   wrangler kv namespace create TOKENS
#   wrangler kv namespace create CACHE
# Then paste the returned ids below.

[[kv_namespaces]]
binding = "TOKENS"
id = "<your-tokens-namespace-id>"

[[kv_namespaces]]
binding = "CACHE"
id = "<your-cache-namespace-id>"

# Set the following secrets:
#   wrangler secret put GOOGLE_CLIENT_ID
#   wrangler secret put GOOGLE_CLIENT_SECRET
#   wrangler secret put MCP_SHARED_SECRET   # `openssl rand -hex 32`
#   wrangler secret put TELEGRAM_BOT_TOKEN
#   wrangler secret put TELEGRAM_CHAT_ID    # single recipient fallback
#   wrangler secret put TELEGRAM_CHAT_IDS   # optional comma-separated fixed recipients
#     ↳ must be hex: the secret is embedded in the URL path (/mcp/<secret>),
#       so base64's `/` and `+` would break routing.
